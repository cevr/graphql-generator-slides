overwrite: true
schema: "schema.graphql"
documents: "src/**/*.tsx"
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
